<%
    const links = locals.user ? [
        "logout",
        "my-storage"
    ] : [
        "register",
        "login",
    ];
%>

<nav>
    <% links.forEach((link) => { %>
        <a href="/<%= link %>">
            <%= link %>
        </a>
    <% }) %>
</nav>
